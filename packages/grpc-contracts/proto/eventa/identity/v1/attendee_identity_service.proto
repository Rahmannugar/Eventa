syntax = "proto3";

package eventa.identity.v1;

import "eventa/identity/v1/attendee_registration.proto";

service AttendeeIdentityService {
  rpc RegisterAttendee(RegisterAttendeeRequest) returns (RegisterAttendeeResponse);
}
